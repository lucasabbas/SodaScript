#ifndef UTILS_H
#define UTILS_H

#include <string>

// Utility function to check if a character is an operator
bool isOperator(char c);

// Utility function to check if a character is whitespace
bool isWhitespace(char c);

#endif // UTILS_H
